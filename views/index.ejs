<%- include("partial/header") -%>

<div class="div1">
<h1>Home</h1>
<p><%= text %></p>
<% postsContents.forEach(function(post){ %>
    <h1> <%= (post.name).toUpperCase() %></h1> 
    <p> 
        <%= (post.postContent).substring(0,90)+"..."%>
        <a href="post/<%=post.name%>">Read More</a> 
    </p>
<% }) %>
</div>
<div class="compose-button-container">
    <form action="/compose" method="get">
    <button class="compose-button">+</button>
</form>
  </div>
  
   
<%- include("partial/footer") -%>
